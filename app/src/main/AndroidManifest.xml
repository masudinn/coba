<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.buahq.buahq">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/home"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BUAHQ">
        <activity
            android:name=".graphic_sell.GraphicSellActivity"
            android:exported="false" />
        <activity android:name=".laporan.ReportActivity"></activity>
        <activity android:name=".bayar.PaymentDetailActivity" />
        <activity android:name=".bayar.PaymentActivity" />
        <activity android:name=".pesan.OrderCheckoutActivity" />
        <activity android:name=".pesan.OrderActivity" />
        <activity android:name=".produk.ProductAddActivity" />
        <activity android:name=".produk.ProductUpdateActivity" />
        <activity android:name=".produk.ProductActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>